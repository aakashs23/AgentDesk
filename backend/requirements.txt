# Web layer
fastapi[standard]==0.139.2
pydantic-settings==2.14.2

# Data layer
sqlmodel==0.0.39
greenlet==3.3.1  # required by SQLAlchemy's asyncio support
alembic==1.18.5
asyncpg==0.31.0
psycopg2-binary==2.9.12
pgvector==0.5.0

# Auth
python-jose[cryptography]==3.5.0
# ponytail: bcrypt directly, no passlib — passlib 1.7.4 is unmaintained and
# crashes on bcrypt >=5. Hashing is bcrypt.hashpw/checkpw, two lines in Phase 3.
# Add passlib (or argon2-cffi) only if a second hash scheme is actually needed.
bcrypt==5.0.0

# AI orchestration + provider (Phase 5 decision: Gemini 2.5 Flash + gemini-embedding-001)
langchain==1.3.14
langgraph==1.2.9
google-genai==2.12.1
# Supervised classifier (Phase 5 decision: DistilBERT fine-tuned on seed data)
torch==2.13.0
transformers==5.14.1

# Dev
# ponytail: ruff only — `ruff format` is black-compatible, so black adds nothing.
ruff==0.15.22
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
